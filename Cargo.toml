[package]
name = "mabm"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4", features = ["clock", "std"] }
csv = "1.3"
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
toml = "0.8"
